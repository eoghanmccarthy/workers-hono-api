export interface Post {
    content: string
    id: number
    title: string
}

export type Bindings = {
    PASSWORD: string,
    USERNAME: string
}

export type Param = {
    content: string
    title: string
}
